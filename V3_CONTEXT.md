# V3 压缩上下文

这份文件是 V3 后续工作的唯一上下文基线。旧对话和 V2 课程细节不再作为
默认设计依据。

## 1. 学习者

- Java 开发经验约 8 年。
- 学习目的同时包含：体系化掌握、真实编码、生产实践和中高级面试。
- 不需要面向初学者解释 Java 基础语法。
- 需要理解“为什么”，不能只背 API 或面试答案。

## 2. 固定学习方式

- 一次只开发和学习一课。
- 每课必须明确区分：
  - `DEMO`：只读、运行和观察；
  - `TASK`：学习者亲手编写；
  - `TEST`：自动验收；
  - `REVIEW`：面试口述和复盘。
- 作业文件不得预先放入参考答案。
- 运行成功不是并发正确性的证明；必须给出同步规则、确定性测试或压力测试证据。
- 讲义 Markdown 直接呈现在网页中，不要求在网页与文档之间来回切换。
- 网页保留 Todo、源码联动、配色切换和学习记录。
- 动画必须准确表达线程栈、共享堆、对象字段、等待队列以及数据更新，不能做成静态 PPT。

## 3. V3 技术基线

- Java 21。
- Maven。
- JUnit 5。
- 需要时使用 JMH、jcstress、`jcmd`、线程转储和 JFR。
- 参考《Java并发编程的艺术》的知识结构，但所有 API 和实现说明按当前 JDK
  重新核对。
- 新增原书出版后已经稳定进入 Java 的内容，例如虚拟线程。
- 小林 Coding JUC 页面识别出的 68 道问题只作为面试覆盖检查表，不复制来源
  答案；课程结论必须由规范、当前 API、代码和测试重新建立。

## 4. V3 课程结构

- 课程数量：8 课。
- 总体学习量：约 27 小时，不再强行压缩为速成课。
- 主项目：`TaskHub` 并发任务中心。
- 每课既有一个最小原理实验，也会给同一个 `TaskHub` 增加一项能力。
- 第八课完成生产级整合，而不是临时拼接一个新项目。
- 68 道来源面试题已经全部分配到唯一主课次：
  `6 / 14 / 6 / 10 / 12 / 5 / 13 / 2`。
- 题库未覆盖的 ConcurrentHashMap 原子复合操作、BlockingQueue 背压、
  LongAdder、CompletableFuture、虚拟线程、JFR、JMH、jcstress、Deadline
  和 Bulkhead 仍由 V3 原创补齐。

## 5. 已废弃的 V2 决策

- 废弃“6 课快速面试版”。
- 废弃 `course01～course06` 六套互相独立的完成版示例。
- 废弃作业代码中预置答案的做法。
- 废弃一课同时塞入过多并列主题但不说明主任务的做法。
- 废弃把演示代码、作业代码和答案代码放在同一层级的目录结构。

## 6. 当前状态与下一步

当前状态：

- 8 课结构、`TaskHub` 主线和约 27 小时学习量已经确认。
- 原书映射已经完成。
- 68 道来源面试题的课次映射和自动完整性校验已经完成。
- V3 源码、测试、讲义、复盘和分屏网页骨架已经建立。
- 第 01 课的丢失更新与死锁 `DEMO`、不安全统计 `TASK`、自动测试、源码联动
  动画和 Markdown 双页签已经完成。
- 默认 `mvn test` 只运行完成态 `DEMO` 并保持绿色；学习者显式运行
  `UnsafeTaskStatisticsAcceptance` 完成作业验收。

唯一实施顺序：

1. 学习者按照网页和 `lesson01.md` 完成第 01 课。
2. 学习者填写 `UnsafeTaskStatistics` 的两个 TODO，并通过显式作业验收。
3. 学习者把实验证据、面试口述和有价值的问题写入 `lesson01-review.md`。
4. 第 01 课由学习者确认验收后，才开始第 02 课。
