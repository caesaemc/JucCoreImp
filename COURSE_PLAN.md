# Java JUC 系统课程 V3 总体设计

> 状态：总体设计已确认；第 01 课已交付，等待学习验收
>
> 参考骨架：《Java并发编程的艺术》
>
> 技术基线：Java 21、Maven、JUnit 5
>
> 学习者：约 8 年 Java 经验，目标为原理、编码、生产实践与面试

## 一、课程最终目标

V3 不以“记住多少个 JUC 类”为目标，而是训练一条完整的并发推理链：

```text
识别共享状态
→ 描述线程之间的关系
→ 选择同步协议
→ 证明内存可见性和原子性
→ 控制任务与资源边界
→ 用测试和运行证据验证
→ 在线上故障中定位问题
```

学完后，学习者应当能：

1. 从堆、线程栈、对象字段和 happens-before 关系解释并发行为。
2. 分清可见性、原子性、有序性、互斥、协作和资源限制。
3. 正确使用并解释 `volatile`、`synchronized`、CAS、原子类、AQS、
   `Lock`、并发容器、同步工具和 Executor。
4. 设计有界线程池、取消协议、超时预算和降级策略。
5. 在平台线程与虚拟线程之间做有依据的选择。
6. 使用确定性测试、jcstress、JMH、线程转储和 JFR 建立证据。
7. 用一套稳定结构回答中高级并发面试题：结论、原理、边界、代码证据。

## 二、课程主线

课程不是 8 个互不相关的 Demo，而是两条线同时推进。

### 原理实验线

每课都有一个可以稳定复现问题的最小实验，用来观察：

- 哪些数据属于线程私有；
- 哪些对象被多个线程共享；
- 读取、写入、CAS、入队、阻塞和唤醒发生在哪里；
- 正确性来自哪一条同步规则。

### 累积项目线

从第一课开始持续开发同一个 `TaskHub` 并发任务中心：

```text
任务提交者
    ↓
任务注册表
    ↓
有界任务通道
    ↓
执行策略（平台线程池 / 虚拟线程）
    ↓
结果、取消、超时与指标
```

后续课程必须在前一课代码上增加能力，不再每课重新发明一套项目。

## 三、8 课路线

| 课次 | 主题 | 主要参考 | `TaskHub` 本课增量 | 学习者必须亲手完成 | 建议用时 |
|---|---|---|---|---|---|
| 01 | 并发的代价、竞态与证据 | 原书第 1 章 | 建立不安全的任务统计与可重复实验框架 | 稳定复现丢失更新、死锁或资源过载中的至少两个问题 | 2.5 小时 |
| 02 | 线程生命周期、状态、通信与中断 | 原书第 4 章 | 建立可启动、可停止、可等待结束的 Worker | 实现两阶段终止，不吞中断，不依赖 `sleep` 猜时序 | 3 小时 |
| 03 | JMM、重排序与安全发布 | 原书第 2、3 章 | 增加不可变运行配置和安全发布入口 | 实现配置快照，画出 volatile/final/happens-before 关系 | 3.5 小时 |
| 04 | synchronized、CAS、ABA 与原子类 | 原书第 2、7 章 | 增加任务状态机、序号和并发指标 | 分别用监视器和 CAS 维护状态，比较正确性与竞争代价 | 3 小时 |
| 05 | AQS、Lock、Condition 与读写控制 | 原书第 5 章 | 增加可等待的有界任务缓冲区 | 用 `ReentrantLock` 和两个 `Condition` 实现 put/take 协议 | 3.5 小时 |
| 06 | 并发容器、阻塞队列与同步工具 | 原书第 6、8 章 | 增加任务注册表、批次执行和容量闸门 | 使用原子 Map 操作、Queue、Latch/Barrier/Semaphore 完成批次流程 | 3.5 小时 |
| 07 | ThreadPoolExecutor、Executor 与 Future | 原书第 9、10 章 | 增加有界执行器、结果编排和拒绝策略 | 配置线程池，处理 Future 超时、取消、异常和关闭 | 4 小时 |
| 08 | Java 21、可靠性、诊断与综合验收 | 原书第 11 章 + 现代扩展 | 完成平台线程/虚拟线程双策略和生产边界 | 实现 Deadline、Bulkhead、部分结果，并完成线程/JFR 诊断报告 | 4 小时 |

预计总学习时间约 27 小时。可以分散完成，不要求连续速成。

## 四、课程依赖关系

```text
第 01 课：先证明问题存在
    ↓
第 02 课：控制线程的出生、等待和死亡
    ↓
第 03 课：证明线程之间何时能看见数据
    ↓
第 04 课：保证共享状态更新不可被破坏
    ↓
第 05 课：让竞争失败的线程正确等待和唤醒
    ↓
第 06 课：用成熟容器和同步工具组织数据流
    ↓
第 07 课：把任务交给有边界的执行框架
    ↓
第 08 课：形成可上线、可取消、可诊断的系统
```

任何一课都必须回答两个问题：

1. 它解决了上一课留下的什么具体缺陷？
2. 它为下一课提供了什么代码基础？

## 五、每课固定教学结构

每课页面和仓库都使用相同的七段式流程。

### 1. 本课交付物

页面第一屏只说明：

- 今天最终要写哪个类；
- 从哪个测试开始；
- 完成后 `TaskHub` 新增什么能力；
- 预计需要多长时间。

### 2. 反例实验

先运行错误代码，稳定构造问题。不能使用“可能偶尔出现”作为唯一证据。

### 3. 内存与运行时动画

动画逐步展示：

- 线程栈中的局部变量；
- 堆中的共享对象和字段；
- CPU/JMM 教学模型中的读取与写入；
- 锁拥有者、AQS 等待队列或任务队列；
- 当前步骤真正变化的数据。

动画必须支持播放、暂停、单步、重置和步骤解释。

### 4. 原理讲义

讲义只承担三个职责：

- 解释反例为什么成立；
- 给出修复协议和边界；
- 连接原书章节、当前 JDK 与面试问题。

### 5. 编码任务

学习者只修改标记为 `TASK` 的文件。初始文件包含接口、必要注释和 TODO，
但不包含最终实现。

### 6. 自动验收

每课至少包含：

- 一个确定性功能测试；
- 一个并发时序测试；
- 一个失败或取消路径测试；
- 必要时增加 jcstress 或 JMH 实验。

### 7. 面试与复盘

每道题使用固定回答结构：

```text
结论
→ 底层原因
→ 适用边界
→ 本课代码或测试证据
```

有价值的问题写回本课学习档案。

## 六、源码角色必须隔离

V3 计划使用以下结构：

```text
src/main/java/com/caesaemc/juc/v3/
├── labs/                  # DEMO：原理反例，只读与运行
│   ├── lesson01/
│   └── ...
└── taskhub/               # TASK：同一个累积项目

src/test/java/com/caesaemc/juc/v3/
├── labs/                  # DEMO 的确定性证明
└── taskhub/
    ├── lesson01/
    └── ...

docs/v3/lessons/           # 每课唯一讲义和学习记录
web/                       # V3 学习网页
```

参考答案不进入学习分支。学习者提交作业并完成评审后，再生成对应课次的答案
标签或独立分支。

## 七、网页信息架构

桌面端继续使用左右分屏，但职责重新定义：

```text
左侧约 65%                         右侧约 35%
────────────────────────────────  ───────────────────────────────
本课交付物                          当前讲义
Todo                               本步骤原理
运行时动画                          原书章节映射
DEMO 源码                           面试与学习记录
TASK 编辑入口
TEST 命令与结果
```

页面中的每个源码卡片必须显示角色标签：

- `只读 DEMO`
- `你来写 TASK`
- `自动验收 TEST`
- `答案未解锁`

这样学习者进入任何一课都能立即知道“我现在应该看什么、写什么、运行什么”。

## 八、V3 的现代化边界

### 保留并深入

- JMM、happens-before、volatile 和 final 语义；
- synchronized、CAS、AQS 和常用 Lock；
- ConcurrentHashMap、BlockingQueue 和同步工具；
- ThreadPoolExecutor、Executor、FutureTask；
- 死锁、资源限制、取消和生产排障。

### 需要按 Java 21 重讲

- 对象头和锁优化只讲当前仍有教学价值的部分；
- 旧版 ConcurrentHashMap 分段锁只作为历史对比；
- 不把特定旧版 JVM 的实现细节描述为 Java 语言保证；
- 线程池规则与当前 API、实际源码和实验结果对应。

### 新增

- `CompletableFuture` 的组合、异常与取消；
- 虚拟线程和每任务一线程执行器；
- 平台线程、虚拟线程与真实下游容量的关系；
- Deadline、Bulkhead、部分结果协议；
- JFR、线程转储、JMH、jcstress。

## 九、总体完成标准

V3 全部完成时，必须同时满足：

- 8 份讲义、8 组动画和 8 组增量代码一一对应；
- 来源页面的 Q01～Q68 每题只归属一个主课次，完整性校验持续通过；
- `TaskHub` 能从第一课代码连续演进到最终版本；
- 学习分支中没有预置答案；
- 所有并发测试可重复运行；
- 每个同步选择都有明确的内存或线程协作解释；
- 每课都有适合 8 年经验开发者的设计题和面试题；
- 最终项目可以演示正常、超时、拒绝、取消和部分失败路径；
- 网页能从同一份 Markdown 渲染讲义与学习记录。

## 十、面试题覆盖基线

小林 Coding 的 JUC 页面共识别出 68 道三级标题问题。V3 保留题目主题，但不
复制其答案；每道题只分配给一个主课次，并按照“结论、规范/API 语义、运行
机制、适用边界、代码与测试证据”重新作答。

| 课次 | 题数 | 面试主线 |
|---|---:|---|
| 01 | 6 | 风险、正确性策略、死锁与竞态证据 |
| 02 | 14 | 线程创建、状态、通信、等待与中断 |
| 03 | 6 | JMM、volatile、重排序与安全发布 |
| 04 | 10 | synchronized、CAS、ABA 与原子更新 |
| 05 | 12 | Lock、AQS、Condition、公平性与锁选型 |
| 06 | 5 | JUC 分类、同步工具与批次协作 |
| 07 | 13 | ThreadLocal、线程池、Future、关闭与取消 |
| 08 | 2 | 平台线程、虚拟线程与现代线程模型 |
| **合计** | **68** | 来源问题全部覆盖 |

详细题目、重复题合并关系、Java 21 校正点及题库缺口见
[V3 JUC 面试题覆盖矩阵](docs/v3/INTERVIEW_MAPPING.md)。

完整性校验命令：

```bash
node scripts/verify-v3-interview-map.mjs
```

## 十一、已确认决策与实施顺序

已经确认：

1. 从 V2 的 6 课调整为 V3 的 8 课。
2. 使用同一个 `TaskHub` 项目逐课演进。
3. 采用约 27 小时的系统学习量，不做 10 小时速成。

V3 基线和第 01 课已经完成课程建设。接下来由学习者运行 `DEMO`、完成
`UnsafeTaskStatistics` 的两个 TODO、通过显式验收并填写学习记录；得到学习者
确认后，才进入第 02 课。
